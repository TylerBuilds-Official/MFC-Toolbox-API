ArtifactId not populating into Messages table. Artifacts generating all other fields fine, just not being inserted into Messages. If a message creates an artifact, it needs to populate the ArtifactId field.


ADMIN PAGE INTEGRATIONS - ADMIN TOOLS